import { Input } from "@/components/ui/input"

const SearchBar = () => {
  return (
  
        <Input type="search" placeholder="Search movies and tv shows" className="text-white w-70 h-10"/>
   
  )
}

export default SearchBar